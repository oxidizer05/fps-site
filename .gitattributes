# Проект правится и на macOS, и на Windows, а папка синхронизируется Яндекс.Диском.
# Без этого файлы, сохранённые на Windows, приезжают с другими переносами строк —
# и git показывает весь файл изменённым, хотя содержимое то же.
* text=auto eol=lf
